<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.maplocation">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name=".GeofenceTrasitionService" />

        <receiver android:name=".GeofenceReciever"
            android:permission=".permission.A4S_SEND">
            <intent-filter>
                <action android:name="com.ad4screen.sdk.intent.action.TRIGGER" />
                <category android:name="com.ad4screen.sdk.intent.category.GEOFENCE_NOTIFICATIONS" />
            </intent-filter>
        <intent-filter>
            <action android:name="com.example.SendBroadcast" >
            </action>
            <action android:name="android.intent.action.ACTION_POWER_DISCONNECTED" >
            </action>
        </intent-filter>
    </receiver>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDZXdF-7ZVBhuWZ--M-VQx7b6sr8ZXGmZ0" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".GetMapLocationActivity">

        </activity>
        <activity android:name=".login.LoginActivity">

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".registeruser.RegisterUserActivity"></activity>
    </application>

</manifest>